Exemple de tests pour voir si tout marche

-----------------------------------------

Vérifier que toutes les routes existent:
docker exec R_FAI_1 vtysh -c "show ip route"
docker exec Box_B1 vtysh -c "show ip route"

Client B1 vers R_FAI_1
docker exec Client_B1 ping -c 4 120.0.32.1

Client B1 vers Client C1 (comme dhcp pas censé marché ou coup de chance)
docker exec Client_B1 ping -c 4 192.168.2.2

Client B1 vers R11
docker exec Client_B1 ping -c 4 120.0.35.2

test DNS et site web
docker exec Client_B1 nslookup www.siteglobal.com
docker exec Client_B1 wget -qO- http://www.siteglobal.com